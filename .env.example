# ── API Keys ──────────────────────────────────────────
TAVILY_API_KEY=
HUNTER_API_KEY=
HIBP_API_KEY=
SHODAN_API_KEY=
VIRUSTOTAL_API_KEY=

# ── Phase 1: Profile Verification ──────────────────────
# Minimum confidence score (0–100) for a profile to be accepted
VERIFICATION_MIN_CONFIDENCE=60
# Levenshtein similarity threshold (0–1) for name matching
VERIFICATION_NAME_SIMILARITY_THRESHOLD=0.7
# Set to "false" to disable profile verification entirely
VERIFICATION_ENABLED=true
